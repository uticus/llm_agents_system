# Unused pipeline components

These files are inactive because the project has no C++ and no pybind11 bindings. They
remain here for reference and can be restored if that ever changes.

- `rules/cpp.md`, `rules/abi.md`, `rules/bindings.md`, `rules/hotpath.md` — C++/binding/hot-path rules
- `skills/impl-cpp.md`, `skills/pybind.md` — C++/binding implementation skills
- `agents/implementer-cpp.md` — C++ implementer role

The Implementer: C++ row in `CLAUDE.md` is therefore inactive.

Note: the ML files (`rules/ml.md`, `skills/impl-ml.md`, `skills/metrics.md`,
`agents/implementer-ml.md`) were restored to active use — the platform now includes local
inference, fine-tuning, embeddings, and RAG models.
